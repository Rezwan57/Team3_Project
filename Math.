class Math:
    def add(self, a, b):
        """Returns the sum of two numbers."""
        return a + b
    
    def Max(self, a, b):
        """Returns the maximum of two numbers."""
        return a if a > b else b
